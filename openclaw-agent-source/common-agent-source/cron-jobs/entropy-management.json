{
  "id": "entropy-management",
  "nameTemplate": "csflow-entropy-management-{agent_id}",
  "schedule": {
    "expr": "0 3 * * 1",
    "tz": "UTC"
  },
  "session": "isolated",
  "messageTemplate": "Run the weekly \"entropy management\" inspection: clean outdated, incorrect, redundant, or duplicated content in the current workspace; maintain and update each `INDEX.md` under `my-desktop/` according to the current directory structure (create missing files); output a concise change summary and include absolute paths of key modified files. Current agent: {agent_id}."
}
